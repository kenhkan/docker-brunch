{
  "name": "docker-brunch",
  "version": "0.0.1",
  "description": "Adds Docker support to brunch",
  "main": "./lib/index",
  "author": "Kenneth Kan",
  "license": "MIT",
  "dependencies": {
    "coffee-script": "1.3.3",
    "docker": "0.2.8"
  },
  "scripts": {
    "install": "node_modules/.bin/coffee -o lib src",
    "test": "node_modules/.bin/mocha --colors --require test/common.js"
  },
  "engines": {
    "node": "~0.10.0"
  }
}
